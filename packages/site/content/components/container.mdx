---
title: Container
description: Fundamental layout element that contains, pads, and centers content within a given viewport.
toc: true
---

import { sassBreakpointSize, sassContainerSize } from '@libs/sass'
import { Example as BasicExample } from '../../examples/components/container/BasicExample.tsx'
import { Example as ResponsiveExample } from '../../examples/components/container/ResponsiveExample.tsx'
import { Example as FluidExample } from '../../examples/components/container/FluidExample.tsx'

## Overview

`<Container>` is the most basic layout element in Chassis, and is **required when using the default grid system**. It contains, pads, and (sometimes) centers the content inside it. Containers can be nested, though most layouts don't need a nested container.

Chassis comes with three container variants:

- `<Container>`, which sets a `max-width` at each responsive breakpoint
- `<Container fluid>`, which is `width: 100%` at all breakpoints
- `<Container fluidUntil={breakpoint}>`, which is `width: 100%` until the specified breakpoint

Each variant's `max-width` follows a different pattern across breakpoints, shown here against the plain `<Container>` and `<Container fluid>` baselines.

<CxTable>
|  | Extra sm<div class="font-normal">&lt;{sassBreakpointSize('sm')}</div> | Small<div class="font-normal">&ge;{sassBreakpointSize('sm')}</div> | Medium<div class="font-normal">&ge;{sassBreakpointSize('md')}</div> | Large<div class="font-normal">&ge;{sassBreakpointSize('lg')}</div> | X-Large<div class="font-normal">&ge;{sassBreakpointSize('xl')}</div> | 2x-Large<div class="font-normal">&ge;{sassBreakpointSize('2xl')}</div> |
| --- | --- | --- | --- | --- | --- | --- |
| `<Container>` | <span class="fg-subtle">100%</span> | {sassContainerSize('sm')} | {sassContainerSize('md')} | {sassContainerSize('lg')} | {sassContainerSize('xl')} | {sassContainerSize('2xl')} |
| `<Container fluidUntil="sm">` | <span class="fg-subtle">100%</span> | {sassContainerSize('sm')} | {sassContainerSize('md')} | {sassContainerSize('lg')} | {sassContainerSize('xl')} | {sassContainerSize('2xl')} |
| `<Container fluidUntil="md">` | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | {sassContainerSize('md')} | {sassContainerSize('lg')} | {sassContainerSize('xl')} | {sassContainerSize('2xl')} |
| `<Container fluidUntil="lg">` | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | {sassContainerSize('lg')} | {sassContainerSize('xl')} | {sassContainerSize('2xl')} |
| `<Container fluidUntil="xl">` | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | {sassContainerSize('xl')} | {sassContainerSize('2xl')} |
| `<Container fluidUntil="2xl">` | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | {sassContainerSize('2xl')} |
| `<Container fluid>` | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> | <span class="fg-subtle">100%</span> |
</CxTable>

## Default container

The default `<Container>` class is a responsive, fixed-width container — its `max-width` changes at each breakpoint.

<Example class="cxd-example-container">
  <BasicExample client:load />
</Example>

## Responsive containers

`<Container fluidUntil={breakpoint}>` renders full width until the given breakpoint is reached, then applies a `max-width` at that breakpoint and every larger one. `<Container fluidUntil="sm">`, for example, stays 100% wide until the `sm` breakpoint, then scales up through `md`, `lg`, `xl`, and `2xl`.

<Example class="cxd-example-container">
  <ResponsiveExample client:load />
</Example>

## Fluid containers

Use `<Container fluid>` for a full width container, spanning the entire width of the viewport.

<Example class="cxd-example-container">
  <FluidExample client:load />
</Example>

## API

### Container

<PropTable component="Container" />
