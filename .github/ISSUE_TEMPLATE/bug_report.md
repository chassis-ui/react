---
name: Bug report
about: Report a defect in @chassis-ui/react
title: ''
labels: bug
assignees: ''
---

## What happened

<!-- What the component did. Include the exact error message or console output if there was one. -->

## What you expected

## Reproduction

<!--
A minimal example we can run. A CodeSandbox/StackBlitz link is ideal; a self-contained snippet is
fine. Please reduce it to the smallest thing that still shows the problem — a whole application is
much slower for us to work through than fifteen lines.
-->

```tsx
import { Button } from '@chassis-ui/react'

// ...
```

## Environment

| | |
| --- | --- |
| `@chassis-ui/react` | <!-- e.g. 0.1.0 --> |
| `@chassis-ui/css` | <!-- e.g. 0.4.0 --> |
| `react` / `react-dom` | <!-- e.g. 19.2.0 --> |
| Browser | <!-- e.g. Chrome 131, Safari 18 --> |
| Bundler / framework | <!-- e.g. Next.js 15 App Router, Vite 6 --> |

## Anything else

<!--
If it's a styling problem, say whether you've imported `@chassis-ui/css` — the components carry
chassis-css class names and ship no styles of their own (except `@chassis-ui/react/style.css` for
the calendar family). If it's an accessibility problem, please say which assistive technology.
-->
